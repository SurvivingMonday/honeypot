var app = {};

app.prototype = function () {
//push element and delete element and transfering between market    
    // Variables here
    var player = {};
    player.cash = 0;
    player.inventory = [];
	alert(player.cash);
	// 3 Types of Keylogger( Beginner, Medium, Hard)
	// hitfactor and risk to be generate depending on type.
	// Keylogger Begin
	var Begin = {};
	Begin.price = 100; 
	Begin.risk = 0.2; 
	Begin.hitfactor = 0.4;
	
	var blackm = {};// arrays to retain everything
	blackm.credentials=[];
	blackm.creditcard=[];
	blackm.unencrypteddata=[];
	blackm.identity=[];
	blackm.keylogger=[];
	for
	blackm.keylogger.push("keyloggerA": Begin);
	

	// Keylogger Medium
	keylogger.Medium ={};
	keylogger.Medium.price = 200;
	keylogger.Medium.risk = 0.4;
	keylogger.Medium.hitfactor = 0.6;
	
	// Keylogger Hard
	keylogger.Hard ={};
	keylogger.Hard.price = 400;
	keylogger.Hard.risk = 0.6;
	keylogger.Hard.hitfactor = 0.8;

	var asset ={}; // Assets Uncrypted data(credentials,uncrypted data)	
	asset.uncrypteddata=0;
	asset.credentials=0;
		
 // Private functions
 

    return {
        // Public functions
        getCash: function () {    //display cash
            return player.cash;    
        },
		buy: function(keylogger){   // buy from
		//checking if keylogger
			player.cash-=player.inventory.price;
			return  
		}
	}
}
